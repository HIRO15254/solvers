{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the embedded Solvers UI.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
