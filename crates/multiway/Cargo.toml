[package]
name = "multiway"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Sampled 2-9 player NLHE solving with side pots and tournament utility"

[features]
default = []
# Gates only the retired full-recall/sparse solver path, which production
# configs already reject with MWP002. Nothing enables it; it exists so the
# sparse path stays compiled until it is deleted outright.
research-abstractions = []

[dependencies]
abstraction = { workspace = true }
cards = { workspace = true }
blake3 = { workspace = true }
postcard = { workspace = true }
rand = { workspace = true }
rand_chacha = { workspace = true }
rayon = { workspace = true }
rustc-hash = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tempfile = { workspace = true }
thiserror = { workspace = true }
zstd = { workspace = true }

[dev-dependencies]
libc = { workspace = true }
toml = { workspace = true }




